<?php
/**
 * Batch Processor Class
 * کلاس برای مدیریت پردازش‌های دسته‌ای
 *
 * نکته: در این پیاده‌سازی، منطق پردازش دسته‌ای مستقیماً در کلاس WC_Reviews_Exporter_Export_Handler
 * و متد process_reviews_in_batches() قرار داده شده است تا از پیچیدگی غیرضروری جلوگیری شود.
 * این فایل به عنوان یک placeholder برای آینده باقی می‌ماند اگر نیاز به سیستم
 * پردازش دسته‌ای پیچیده‌تر و قابل استفاده مجدد در بخش‌های دیگر افزونه باشد.
 */

if (!defined('ABSPATH')) {
    exit;
}

class WC_Reviews_Exporter_Batch_Processor {

    // در حال حاضر بدون متدها یا ویژگی‌های خاص.
    // منطق پردازش دسته‌ای در WC_Reviews_Exporter_Export_Handler::process_reviews_in_batches() قرار دارد.

}
